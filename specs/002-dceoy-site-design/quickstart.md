# Quickstart

## Local development

1. Install dependencies:
   - `pnpm install`
2. Start dev server:
   - `pnpm dev`
3. Open the app at the Vite URL shown in the terminal.

## Production preview

1. Build:
   - `pnpm build`
2. Preview:
   - `pnpm preview`
